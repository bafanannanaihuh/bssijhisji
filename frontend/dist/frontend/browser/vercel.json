{
  "buildCommand": "npm run build || echo 'Using prebuilt dist'",
  "outputDirectory": "dist/frontend/browser",
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/.*",
      "dest": "/index.html"
    }
  ]
}
