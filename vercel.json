{
  "buildCommand": "cd frontend && npm install && npm run build || echo 'Using prebuilt dist'",
  "outputDirectory": "frontend/dist/frontend/browser",
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/.*",
      "dest": "/index.html"
    }
  ]
}

